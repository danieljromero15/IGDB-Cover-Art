A simple Python script to get the cover art for a video game

Requires a .env file with `CLIENT_ID` and `CLIENT_SECRET`

Required Python Packages:

* `requests`
* `python-dotenv`
* `datetime`
* `webbrowser`